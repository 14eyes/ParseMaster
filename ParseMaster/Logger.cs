namespace ParseMaster;

public static class Logger
{
    public static void Write(object format)
    {
        //Console.Write(format.ToString());
    }

    public static void WriteLine(object format)
    {
        //Console.WriteLine(format.ToString());
    }
}